# --- Core Django ---
Django>=5.0,<6.0

# --- API Framework ---
djangorestframework>=3.14

# --- CORS Headers (для связи с React) ---
django-cors-headers>=4.3.1

# --- Database (Опционально, но полезно) ---
# Если проект будет использовать PostgreSQL в будущем:
# psycopg2-binary